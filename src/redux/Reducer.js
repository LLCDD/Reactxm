
var Reducer = function(state,action){
    if(typeof state === "undefined"){
        return {}
    }
    switch (action.type) {
        case "":
            
           return state
    
        default:
            return state
    }
}


export default Reducer;